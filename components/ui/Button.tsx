import React from 'react';

const Button = () => {
  return (
    <button className="text-white bg-accent font-primary px-5 py-2 hover:bg-yellow transition-all duration-200">
      send
    </button>
  );
};

export default Button;
